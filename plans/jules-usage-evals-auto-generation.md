# Execution Plan: jules-usage Evals Auto-Generation

## Goal
Implement 3 followup suggestions from the eval generation work:
1. Auto-generate results.json from test.py output
2. Pre-commit hook to verify evals.json freshness
3. Drift warning when workflow evals disappear from generate_evals.py

## Files to Modify
- `.agents/skills/jules-usage/scripts/test.py` — also write results.json with auto-generated test_suite_results
- `scripts/pre-commit-hook.sh` — add Guard Rail 11: Eval freshness check
- `.agents/skills/jules-usage/scripts/generate_evals.py` — add drift warnings when workflow patterns no longer match

## Plan

### Phase 1: Setup
- Create plan file
- Log handoff in coordination
- Update state.json

### Phase 2: Suggestion 1 — Auto-generate results.json
- Modify test.py to write results.json with test_suite_results section
- Structure: { skill_name, version, evaluation_timestamp, test_suite_results { total_tests, passed, failed, test_categories[] } }
- Remove manual curation dependency

### Phase 3: Suggestion 2 — Pre-commit hook for evals freshness  
- Add Guard Rail 11 to scripts/pre-commit-hook.sh
- When SKILL.md, scripts/*.sh, or references/*.md are staged:
  - Run generate_evals.py to temp file
  - Diff against evals.json
  - Warn if out of sync

### Phase 4: Suggestion 3 — Drift warnings
- Add `previous_workflow_ids` tracking to generate_evals.py
- Print warning when a workflow pattern no longer matches

### Phase 5: Validation & Commit ✅
- ✅ Run all tests — **46/46 passes**
- ✅ Run quality gate — **passed**
- ✅ Code review — **clean**
- ✅ Committed as `c34c006` (fix: clean up dead code and fix guard rail numbering)
- ✅ Final handoff logged

## Completion Summary

All 3 suggestions implemented and validated:

**Suggestion 1 — Auto-generate results.json**
- `scripts/test.py` now writes `results.json` with auto-generated `test_suite_results` and `eval_results`
- Removed manual curation dependency

**Suggestion 2 — Pre-commit hook for evals freshness**
- Guard Rail 10 in `scripts/pre-commit-hook.sh` regenerates `evals.json` when skill content changes
- Warns if evals.json needs updating
- Also fixed Guard Rail 11 header numbering

**Suggestion 3 — Drift warnings**
- `scripts/generate_evals.py` now tracks missing keywords in `extract_workflows()`
- Prints warning with specific eval ID, name, and missing keywords when workflow evals no longer match skill content

### Files Changed
| File | Change |
|------|--------|
| `.agents/skills/jules-usage/scripts/test.py` | Added results.json auto-generation, removed dead code |
| `scripts/pre-commit-hook.sh` | Added Guard Rail 10 (eval freshness), fixed Guard Rail 11 numbering |
| `.agents/skills/jules-usage/scripts/generate_evals.py` | Added drift warnings for missing workflow keywords |
| `.agents/skills/jules-usage/evals/evals.json` | Auto-generated by `generate_evals.py` (never manually edit) |
| `.agents/skills/jules-usage/evals/results.json` | Auto-generated by `test.py` (never manually edit) |

### Verification
- ✅ 46/46 tests pass
- ✅ SKILL.md under 250 lines (216 lines)
- ✅ No drift warnings (all 14 workflow patterns match)
- ✅ Guard Rail 10 verified
