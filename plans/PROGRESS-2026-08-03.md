# Progress Report — 2026-08-03

**Generated**: 2026-08-03
**Branch**: `feat/pwa-dashboard`
**Status**: E2E stabilized; PR #662 pushed and CI green

## E2E Stabilization (follow-up to the PWA batch)

The CI-mirrored local E2E run surfaced five root causes on the PR head; all were fixed in commit `918bdeb` and documented in ADR-021:

- **Idempotent Phase 4 registration**: accepts `201` (fresh) or `400` already-registered so pre-seeded runs are deterministic.
- **Deterministic D1 pre-seeding**: `setup-auth.sh` now initializes D1 to version 9 and registers/promotes the three E2E fixtures before Playwright starts.
- **API-key tests decoupled from the login rate limit**: the seeded admin `X-API-Key` replaces a login-minted JWT in the key-CRUD `beforeAll`.
- **`listApiKeys` keyHash fallback**: derives `keyHash` from the KV key name when metadata lacks it (fixes rotate/revoke 404s).
- **Deals snapshot seeding**: a `SnapshotSchema`-valid 4-deal fixture is put into local and CI KV (fixes `/deals` 404).
- **Expired-key rotation edge**: past `expiresAt` is skipped on rotate and `storeApiKey` never passes a past/invalid expiration to KV (fixes rotate 500).

Also: extracted `worker/routes/auth-bookmarks.ts` (auth.ts slimmed), hardened the D1 SQL splitter against comment-only statements, and added migration/factory unit tests.

## Verification

- `npm run lint` passed (tsc + repository-wide Prettier) — re-confirmed 2026-08-03.
- Worker-side E2E 42/42 green; d1+auth unit tests 281/281.
- CI: 24 checks green — E2E Tests, Unit Tests, Run Tests, Quality Gate, Build, Type Check, Format Check, Smoke Tests, Validation Gates, Workers Builds, and more.
- The 22 remaining local failures are browser-extension E2E tests requiring Playwright system deps — environment-only, covered by CI (`npx playwright install --with-deps chromium`).
- Codacy check stays red with no inline GitHub comments (dashboard-only); consistent with documented pre-existing Codacy noise (GOAP_STATE, PR #588).

## Delivery

- PR #662 (`feat/pwa-dashboard` → `develop`) has a What/Why/Impact body covering both the PWA and E2E-stabilization change sets.
- Branch pushed at `918bdeb`; local and remote heads match.
- ADR-021 records the full E2E findings and fixes.
