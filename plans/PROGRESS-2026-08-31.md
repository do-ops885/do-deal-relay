# Progress — 2026-08-31 Full Swarm (v0.19.0)

## What shipped
- **P1 (4)**: auth WeakMap (I-1), D1 write single-attempt (I-2), evolveTrust atomic SQL (I-3), validatedFetch redirect manual (I-4)
- **P2 (4)**: referral raw validation + SSRF + allowlist (I-5/I-6), reactivate audit fix (I-7), referral rate limits (I-8)
- **P3 (2)**: CONFIG synced (I-9), evolveTrustBatch single IN query (I-10)
- **CI**: research-api flake fixed (orchestrator return false in test env)
- **Features (8)**: hybrid search RRF, AI Gateway llm proxy, MCP SSE verified, high-value notifier, referral analytics D1+snapshot fallback, NLQ saved/suggestions (migration 12), real scrapers default, extension feed with alarms/badge/notifications

## Verification
- tsc --noEmit clean, prettier clean, 2633/2633 unit tests green (190 files)
- New migrations: schema-part-6 (nlq_saved_queries, id 12)

## Remaining
- CI-1 E2E/Smoke: still blocked — requires repo owner to add CLOUDFLARE_API_TOKEN secret; workflow would need conditional skip if secret absent (deferred).

## Impact
- Security: 3 SSRF/toctou/redirect vectors closed + auth runtime fix.
- Correctness: trust lost-update and N+1 eliminated; D1 writes no longer double-executed.
- Product: search quality (hybrid), referral ROI visibility, deal alerts, saved queries, extension re-engagement.

