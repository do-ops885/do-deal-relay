<<<<<<< SEARCH
  function toggleSettings() {
    const isActive = elements.settingsPanel.classList.toggle("active");
    elements.manualSection.classList.toggle("hidden");
=======
  function toggleSettings() {
    const isActive = elements.settingsPanel.classList.toggle("hidden");
    // isActive is true if hidden was added, false if removed.
    // So current active state is !isActive
    const isNowVisible = !isActive;
    elements.manualSection.classList.toggle("hidden", isNowVisible);
>>>>>>> REPLACE
<<<<<<< SEARCH
    elements.settingsLink.textContent = isActive ? "Back" : "Settings";
    elements.settingsLink.setAttribute("aria-expanded", isActive.toString());
    elements.settingsLink.setAttribute("aria-pressed", isActive.toString());

    if (isActive) {
=======
    elements.settingsLink.textContent = isNowVisible ? "Back" : "Settings";
    elements.settingsLink.setAttribute("aria-expanded", isNowVisible.toString());
    elements.settingsLink.setAttribute("aria-pressed", isNowVisible.toString());

    if (isNowVisible) {
>>>>>>> REPLACE
