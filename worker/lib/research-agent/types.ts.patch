<<<<<<< SEARCH
export interface ResearchSource {
  name: string;
  baseUrl: string;
  searchPattern: string;
  extractionPatterns: {
    code: RegExp[];
    reward: RegExp[];
    url: RegExp[];
  };
  priority: number;
=======
export interface ResearchSource {
  name: string;
  baseUrl: string;
  searchPattern: string;
  extractionPatterns: {
    code: RegExp[];
    reward: RegExp[];
    url: RegExp[];
  };
  selectors?: {
    code?: string;
    reward?: string;
    url?: string;
  };
  priority: number;
>>>>>>> REPLACE
