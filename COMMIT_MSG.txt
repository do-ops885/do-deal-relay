docs(ci): polish E2E Lint impl pointer per reviewer micro-suggestion

Tightens the trailing `# Implementation: ...` line in the WHY-comment
above `lint-e2e:` to also surface the npm entrypoint, so a maintainer
reading ci.yml alone knows both WHERE the script lives AND how the
job invokes it (without grepping package.json scripts).

No code change, just discoverability.
