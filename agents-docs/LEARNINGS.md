# Lessons Learned - do-deal-relay

| Date | Issue | Root Cause | Prevention |
|------|-------|-----------|------------|
| 2026-06-05 | #423 (round 3): After the round-2 fix, the `Workers Builds: do-deal-relay` check (posted by the `cloudflare-workers-and-pages[bot]` GitHub App, not by any workflow in the repo) still failed `in 0s`. The env `CLOUDFLARE_API_TOKEN` is invalid (HTTP 9109), so `npx wrangler` cannot reach the Cloudflare API and we cannot read the build log to diagnose. | (1) The `Workers Builds: do-deal-relay` PR check is posted by the **Cloudflare GitHub App**, not by anything in this repo. There is no YAML / workflow / repo-level way to disable it. (2) `actionlint` v1.7.12's `if-cond` rule flags **both** `if: false` AND `if: ${{ false }}` as "constant expression in condition. remove the if: section" — the fix is to remove `if:` entirely and use a no-op `run:` step. (3) `npx wrangler` v4.95 will silently use `CLOUDFLARE_API_TOKEN` from the env; if the token is expired or revoked, every command fails with HTTP 9109 "Invalid access token" and there's no fallback to OAuth login. Wrangler's OAuth device-flow needs `xdg-open`, which is missing in this codespace. | (1) **Disconnect the Cloudflare GitHub App** at `https://github.com/settings/installations` → "Cloudflare Workers and Pages" → Configure → remove this repo from "Repository access". One-time UI step; no way to automate from the repo. Documented in `docs/deployment-runbook.md` under "Disconnecting the Cloudflare GitHub App". (2) **GitHub Actions owns `wrangler deploy`**: re-enabled the 4 `cloudflare/wrangler-action` invocations in `deploy-production.yml`, `canary.yml`, `release.yml`, `rollback.yml` (no `if:` section, with `id: deploy` for downstream step references). After app disconnect, the `Workers Builds: do-deal-relay` check will not appear on PRs. (3) When disabling steps permanently, use a no-op `run:` step (with explanatory echo) instead of `if: false` / `if: ${{ false }}` — both forms are flagged by actionlint. (4) If the env `CLOUDFLARE_API_TOKEN` is invalid, the only path is a fresh token (User Profile → API Tokens → Create Token, with `Workers Scripts:Read` + `Workers Builds:Read` scopes). |
| 2026-06-05 | #423: Production deployment failed at `Verify staging is healthy` step (`❌ CLOUDFLARE_WORKER_HOST secret not set` then `❌ Staging not healthy`) → blocked all prod deploys. Also: Cloudflare "Latest build failed" because `wrangler deploy` cannot find the `deal-embeddings` Vectorize index | (1) CI workflow read `WORKER_HOST="${{ secrets.CLOUDFLARE_WORKER_HOST }}"` but the secret was never set. (2) `wrangler.jsonc` declared a Vectorize binding whose index was never created in the Cloudflare account | (1) **Workers.dev URL pattern is `<worker>.<account-subdomain>.workers.dev`.** The `account-subdomain` is a slug chosen at signup (e.g. `do-it-119`), NOT the hex `account_id`. It cannot be derived from `CLOUDFLARE_ACCOUNT_ID` — must be set as `CLOUDFLARE_WORKER_HOST` secret. Use `scripts/worker-host.sh <env>` as the single source of truth; exit 2 with actionable error if missing. (2) When adding a binding to `wrangler.jsonc`, also add a setup workflow (`vectorize-setup.yml` parallel to `kv-setup.yml`) so the resource can be created via Actions UI before the first deploy. |
| 2026-06-04 | When closing #297 (Semantic Search epic) the closure comment claimed "tied to Cloudflare Vectorize bindings in `wrangler.jsonc`" but `wrangler.jsonc` does NOT declare a `vectorize` binding — only `kv_namespaces` and `d1_databases` are configured | Agent verified the type-level integration in `worker/lib/search/types.ts` but did not verify the runtime binding declaration before posting the comment | **Verify claims before publishing them.** When closing an issue with a comment that references specific files, run `grep` / `Read` to confirm those files actually contain what the comment claims. If a discrepancy is found, post a follow-up comment OR create a `plans/FOLLOWUP-<slug>.md` documenting the gap. |
| 2026-06-04 | While fixing Codacy CI issues, did not attempt to fix pre-existing warnings and issues in neighboring files encountered during the work | Focused narrowly on the specific task and ignored surrounding issues that surfaced during analysis | **Fix-Forward rule**: Always attempt to fix pre-existing issues and warnings encountered during work. If a fix is not possible (upstream bug, external dependency, out-of-scope), create a `plans/FOLLOWUP-<slug>.md` documenting the issue and proposed resolution. |
| 2026-06-04 | Created throwaway `scripts/list_codacy_issues.py` to parse Codacy JSON output instead of using the imported `codacy-analysis-cli` skill's documented `jq` commands | Agent loaded skills but did not follow their documented workflows; improvised ad-hoc scripts instead | **Always follow loaded skill workflows.** When a skill is loaded (via `skill()` or available in `.agents/skills/`), use its documented commands, CLI flags, and output-parsing patterns. Never create throwaway scripts for tasks the skill already covers. If a skill's workflow is insufficient, update the skill rather than bypassing it. |
| 2026-06-04 | Guard rails blocked commit because `.agents/skills/` SKILL.md content triggered false positives (coverage/ path strings, jacoco references) | Guard rails scan file content for blocked patterns and secrets, but `.agents/skills/` markdown contains example paths and pattern IDs that match these rules | Add `.agents/skills/` to the guard rails exclusion list for content-based pattern and secret scanning. Skills are documentation, not executable code. |
| 2026-06-03 | E2E + Smoke Tests failed in CI (missing `EMAIL_WEBHOOK_SECRET`) | `validateConfig()` requires `EMAIL_WEBHOOK_SECRET` but CI workflow only passed `WEBHOOK_SECRET` and `API_ENCRYPTION_KEY` to `wrangler dev` | When adding a new required env var to `validateConfig()`, update ALL CI workflows that start `wrangler dev` (ci.yml E2E/Smoke jobs, deploy-staging.yml, deploy-production.yml) |
| 2026-06-03 | E2E metrics test expected Prometheus format, got JSON | Main changed `/metrics` to return JSON by default, E2E tests still expected `text/plain` with `# HELP` | After endpoint behavior changes, update ALL test files (unit, integration, E2E, smoke) that assert on response format |
| 2026-05-20 | PR #324 merge conflicts (6 files) | PR branch and `main` both modified same security/auth files in parallel (`worker/config.ts`, `worker/lib/security.ts`, `worker/lib/research-agent/fetcher.ts`, `worker/routes/referrals.ts`, `worker/index.ts`) | Use the Shared Files Protocol; check active branches before modifying security infrastructure files |
| 2026-05-20 | Post-merge TS errors (test imports of deleted modules, wrong function arity) | Main branch deleted `worker/pipeline/discovery-utils.ts` that PR branch's tests still imported; main added `request` param to `handleGetReferralByCode` | Run `npm run typecheck` and full test suite immediately after merge resolution |
| 2026-06-21 | Cloudflare skill missing evals and exceeding 250-line limit in SKILL.md | Skill file contained an extensive inline product index that pushed it over the line limit; no evals existed for validation | Extract large reference content into `references/` subdirectory files to keep SKILL.md under 250 lines. Add `evals/evals.json` with comprehensive test cases. Use `quick_validate.py` and `check_structure.py` for verification. |

As new lessons are discovered, add them to this table. Keep the table sorted by most recent date first.
