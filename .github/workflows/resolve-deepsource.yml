---
name: "Resolve outdated DeepSource comments"

"on":
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  resolve-outdated:
    name: Resolve Outdated
    runs-on: ubuntu-latest
    steps:
      - name: Resolve outdated DeepSource threads
        uses: Ardiannn08/resolve-outdated-comment@3b3cf4b2651a84fac2d6a94c2aeca9ac7c05ac5f
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          filter-user: "deepsource-io[bot]"
          mode: "resolve"
