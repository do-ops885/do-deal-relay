---
name: "Resolve outdated DeepSource comments"

"on":
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  resolve-outdated:
    name: Resolve Outdated
    runs-on: ubuntu-latest
    steps:
      - name: Resolve outdated DeepSource threads
        uses: Ardiannn08/resolve-outdated-comment@e47f7d14d1887e41530653664d5570081d43a758
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          filter-user: "deepsource-io[bot]"
          mode: "resolve"
