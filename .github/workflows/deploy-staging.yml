---
name: Deploy - Staging

"on":
  push:
    branches: [develop]
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy to"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

concurrency:
  group: deploy-staging
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 15

    if: github.ref == 'refs/heads/develop' || github.event.inputs.environment == 'staging'

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version-file: ".nvmrc"

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Run tests
        run: ./scripts/run-tests-ci.sh

      - name: Deploy to Cloudflare Staging
        uses: cloudflare/wrangler-action@9acf94ace14e7dc412b076f2c5c20b8ce93c79cd
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy --env staging
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Verify staging deployment
        run: |
          echo "Waiting for deployment to propagate..."
          sleep 15

          # Health check
          # Prefer an explicit worker host secret (CLOUDFLARE_WORKER_HOST)
          # e.g. do-deal-relay.do-it-119.workers.dev
          WORKER_HOST="${{ secrets.CLOUDFLARE_WORKER_HOST }}"
          ACCOUNT_ID="${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"

          if [ -n "${WORKER_HOST}" ]; then
            HEALTH_URL="https://${WORKER_HOST}/health"
          else
            HEALTH_URL="https://do-deal-relay-staging.${ACCOUNT_ID}.workers.dev/health"
          fi

          echo "Checking health at: ${HEALTH_URL}"

          for i in {1..5}; do
            if curl -sf "${HEALTH_URL}"; then
              echo "✅ Staging health check passed"
              exit 0
            fi
            echo "Attempt $i failed, retrying..."
            sleep 10
          done

          echo "❌ Staging health check failed after 5 attempts"
          exit 1

      - name: Run smoke tests on staging
        run: |
          # Prefer explicit host secret; fall back to account-derived subdomain
          WORKER_HOST="${{ secrets.CLOUDFLARE_WORKER_HOST }}"
          ACCOUNT_ID="${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"

          if [ -n "${WORKER_HOST}" ]; then
            STAGING_URL="https://${WORKER_HOST}"
          else
            STAGING_URL="https://do-deal-relay-staging.${ACCOUNT_ID}.workers.dev"
          fi

          echo "Running smoke tests on staging (target: ${STAGING_URL})..."

          # Test health endpoint
          curl -sf "${STAGING_URL}/health" || exit 1
          echo "✅ Health endpoint OK"

          # Test metrics endpoint
          curl -sf "${STAGING_URL}/metrics" || exit 1
          echo "✅ Metrics endpoint OK"

          # Test deals endpoint
          curl -sf "${STAGING_URL}/deals" || exit 1
          echo "✅ Deals endpoint OK"

          # Test API status endpoint
          curl -sf "${STAGING_URL}/api/status" || exit 1
          echo "✅ API status endpoint OK"

          echo "All smoke tests passed!"

      - name: Notify on success
        if: success()
        run: |
          WORKER_HOST="${{ secrets.CLOUDFLARE_WORKER_HOST }}"
          ACCOUNT_ID="${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"

          if [ -n "${WORKER_HOST}" ]; then
            URL="https://${WORKER_HOST}"
          else
            URL="https://do-deal-relay-staging.${ACCOUNT_ID}.workers.dev"
          fi

          echo "Staging deployment completed successfully"
          echo "URL: ${URL}"

      - name: Notify on failure
        if: failure()
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        with:
          script: |
            const sha = context.sha;
            const runId = context.runId;
            const url = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}`;
            const title = 'Staging deployment failed';

            // Check for existing open staging issue
            const existingIssues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'deployment,staging,failed'
            });

            const duplicateExists = existingIssues.data.some(issue =>
              issue.title === title && issue.body.includes(sha.substring(0, 7))
            );

            if (duplicateExists) {
              console.log(`Staging issue already exists for ${sha.substring(0, 7)}`);
              return;
            }

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              body: `Staging deployment failed for commit ${sha}.\n\n` +
                `[View workflow run](${url}/actions/runs/${runId})`,
              labels: ['deployment', 'staging', 'failed']
            });
